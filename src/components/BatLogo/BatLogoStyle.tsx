import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  title: {
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 30,
    color: '#E5BF3C'
  },
  image: {
    resizeMode: 'contain',
    height: 180
  }
});