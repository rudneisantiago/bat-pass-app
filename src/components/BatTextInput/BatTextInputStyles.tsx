import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  input: {
    color: 'black',
    backgroundColor: '#E5BF3C',
    fontSize: 20,
    borderWidth: 1,
    borderRadius: 10,
    width: '100%',
    padding: 10,
    textAlign: 'center'
  }
});